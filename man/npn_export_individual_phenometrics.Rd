% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tinybird-export.R
\name{npn_export_individual_phenometrics}
\alias{npn_export_individual_phenometrics}
\title{Download individual phenometrics}
\usage{
npn_export_individual_phenometrics(
  start_date = NULL,
  end_date = NULL,
  species_ids = NULL,
  phenophase_ids = NULL,
  site_ids = NULL,
  individual_ids = NULL,
  states = NULL,
  phenophase_categories = NULL,
  ...,
  as = c("data", "path", "lazy"),
  file_path = NULL,
  wait = TRUE,
  timeout = 300,
  refresh = FALSE
)
}
\arguments{
\item{start_date, end_date}{\strong{Required.} The bounds of the date range, as
\code{Date} objects or \code{"YYYY-MM-DD"} strings. Unlike status data, this product
cannot be requested without them: an unfiltered call would sort tens of
millions of rows on the server.}

\item{species_ids}{Optional vector of species ids to filter on. See
\code{\link[=npn_species]{npn_species()}}.}

\item{phenophase_ids}{Optional vector of phenophase ids to filter on. See
\code{\link[=npn_phenophases]{npn_phenophases()}}.}

\item{site_ids}{Optional vector of site ids to filter on. These match the
\code{site_id} column of the returned data. See \code{\link[=npn_stations]{npn_stations()}}.}

\item{individual_ids}{Optional vector of individual ids to filter on. These
match the \code{individual_id} column of the returned data.}

\item{states}{Optional vector of two-letter state abbreviations. Cannot be
combined with a bounding box.}

\item{phenophase_categories}{Optional vector of phenophase category names.}

\item{...}{Additional filters, passed to the service as-is. Names are checked
against the fields \emph{this data product} accepts and an unrecognized name is
an error, because the service itself silently ignores anything it does not
recognize --- an unchecked typo would widen your query rather than fail it,
and a flag outside a product's own whitelist is dropped just as quietly.
Useful ones include \code{pheno_class_ids}, \code{network_ids}, \code{functional_types},
the \verb{include_*} output-column flags (set them to \code{"1"}), and the bounding
box fields \code{bottom_left_x1}, \code{bottom_left_y1}, \code{upper_right_x2},
\code{upper_right_y2}, which must be supplied all four or not at all.}

\item{as}{What to return. \code{"data"} (the default) returns a tibble; \code{"path"}
returns the path to the downloaded \code{.csv.gz} file without reading it;
\code{"lazy"} returns a duckdb-backed table you can query with dplyr verbs
without loading it into memory. This argument is the \emph{only} thing that
determines the return type. \code{"data"} and \code{"lazy"} use duckdb; set
\code{options(rnpn.engine = "readr")} to parse with readr instead and skip
duckdb entirely, which rules out \code{"lazy"} but needs nothing installed.}

\item{file_path}{Optional path at which to keep a durable copy of the
downloaded \code{.csv.gz}. Independent of \code{as}: you can ask for a tibble and a
saved file in the same call. The file is gzipped CSV, so name it
accordingly. When \code{NULL} (the default) the download is kept only in the
package cache.}

\item{wait}{If \code{TRUE} (the default), block until the export is ready. If
\code{FALSE}, return the job id immediately and collect it later with
\code{\link[=npn_get_job]{npn_get_job()}}.}

\item{timeout}{Seconds to wait before giving up. Timing out is an error, not
a return value, and it does not cancel the job --- the error message tells
you how to collect it later.}

\item{refresh}{If \code{TRUE}, ignore any cached copy of this exact query and
download it again.}
}
\value{
A tibble, a file path, or a lazy table, according to \code{as}. When
\code{wait = FALSE}, the job id as a character scalar.
}
\description{
Requests an individual phenometrics export, waits for the service to build
it, and returns it. One row is one phenological "island": an individual plant
or animal, one phenophase, one onset/end pair.
}
\details{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}} This is a prototype sibling of
\code{\link[=npn_download_individual_phenometrics]{npn_download_individual_phenometrics()}}, not a replacement for it. Its
arguments may change, and the geospatial arguments of the legacy function
(\code{six_leaf_layer}, \code{agdd_layer}, \code{wkt}, ...) have no equivalent here.
}
\section{Column names}{

The service emits this product's columns in legacy \code{PascalCase}
(\code{First_Yes_DOY}). \code{as = "data"} and \code{as = "lazy"} return them lowercased
(\code{first_yes_doy}), which is what the legacy download and every vignette use.
\code{as = "path"} hands over the raw file with the header untouched.
}

\section{Seasonal windows}{

The service decomposes \verb{[start_date, end_date]} into one disjoint window per
phenological year, anchored on the \strong{month-day} of the two dates. A
multi-year range therefore returns that season in each year, not the
continuous span between them:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{npn_export_individual_phenometrics(
  start_date = "2019-01-01",
  end_date = "2021-06-30"
)
# returns Jan 1 - Jun 30 of 2019, of 2020, and of 2021.
# July through December of 2019 and 2020 are NOT in the result.
}\if{html}{\out{</div>}}

This is the legacy function's \code{period_start} / \code{period_end} behavior arriving
through the date arguments instead of through two more of them. A range whose
\code{end_date} falls no later in the year than its \code{start_date} wraps the turn of
the year and must span at least one full year, or it contains no complete
window and is an error.

\code{\link[=npn_export_magnitude_phenometrics]{npn_export_magnitude_phenometrics()}} is \strong{never} windowed; do not carry
this conclusion across to it.
}

\section{Large queries}{

As in \code{\link[=npn_export_status_data]{npn_export_status_data()}}, except that the number is an \strong{estimate}:
no count pipe exists for this product, so the matching observation count is
scaled by a rule of thumb (~20 observations per island). Correct the ratio
with \code{options(rnpn.preflight_ratios = c(individual = 30))}, or switch the
check off with \code{options(rnpn.preflight = FALSE)}.
}

\examples{
\dontrun{
ipm <- npn_export_individual_phenometrics(
  start_date = "2019-01-01",
  end_date = "2019-12-31",
  species_ids = 3
)
}
}
\seealso{
\code{\link[=npn_export_status_data]{npn_export_status_data()}}, \code{\link[=npn_get_job]{npn_get_job()}}
}
